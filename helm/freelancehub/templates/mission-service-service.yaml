apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.missionService.name }}
  labels:
    app: {{ .Values.missionService.name }}
spec:
  selector:
    app: {{ .Values.missionService.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.missionService.port }}
      targetPort: {{ .Values.missionService.port }}
  type: ClusterIP
