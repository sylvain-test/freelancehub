apiVersion: v1
kind: Service
metadata:
  name: mission-service
  labels:
    app: mission-service
spec:
  type: ClusterIP
  selector:
    app: mission-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
